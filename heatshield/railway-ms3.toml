[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -pl agent-service -am -DskipTests -q"

[deploy]
startCommand = "java -Xmx512m -jar agent-service/target/*.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
