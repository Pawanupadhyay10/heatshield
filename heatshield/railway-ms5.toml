[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -pl notification-service -am -DskipTests -q"

[deploy]
startCommand = "java -Xmx512m -jar notification-service/target/*.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
