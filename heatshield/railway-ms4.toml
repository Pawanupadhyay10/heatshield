[phases.setup]
nixPkgs = ["jdk21_headless", "maven"]

[phases.build]
cmds = ["mvn clean package -pl routing-service -am -DskipTests -q"]

[start]
cmd = "java -Xmx512m -jar routing-service/target/*.jar"
