[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -pl routing-service -am -DskipTests -q"

[deploy]
startCommand = "java -Xmx512m -jar routing-service/target/*.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
